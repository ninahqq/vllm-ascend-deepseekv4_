# 边云协同系统下普通请求处理流程（含边云通信与进程间数据传输）

> 展示边侧节点与云侧节点之间的通信，以及节点内部进程间的数据传输机制

---

## 一、系统物理部署与进程架构

```mermaid
flowchart TB
    subgraph Edge["🔷 边侧节点（Edge）"]
        subgraph EF["Frontend 进程"]
            API["API Server<br/>FastAPI / HTTP Server"]
            EC1["EngineCoreClient<br/>(AsyncMPClient / SyncMPClient)"]
        end
        
        subgraph EE1["EngineCore 进程（可选 Prefill）"]
            ECP1["EngineCoreProc"]
            SCH1["Scheduler"]
            EX1["Executor"]
            W1["Worker 0"]
            W2["Worker 1"]
        end
    end
    
    subgraph Cloud["🔶 云侧节点（Cloud）"]
        subgraph EC2P["EngineCore 进程"]
            ECP2["EngineCoreProc"]
            SCH2["Scheduler"]
            EX2["Executor"]
        end
        
        subgraph Workers["Worker 进程池"]
            W3["Worker 0<br/>GPU 0"]
            W4["Worker 1<br/>GPU 1"]
            W5["Worker N<br/>GPU N"]
        end
    end
    
    %% 边云通信（网络层）
    EC1 -.->|"ZMQ TCP Socket<br/>🌐 网络传输<br/>请求 / 响应"| ECP2
    ECP1 -.->|"KV Transfer<br/>🌐 网络传输<br/>（P/D 分离时）"| ECP2
    
    %% 边侧内部 IPC
    API -->|"Python 函数调用"| EC1
    EC1 -->|"ZMQ IPC Socket<br/>📡 进程间通信"| ECP1
    ECP1 -->|"直接调用 / Queue"| EX1
    EX1 -->|"MessageQueue<br/>📬 Shared Memory"| W1
    EX1 -->|"MessageQueue<br/>📬 Shared Memory"| W2
    
    %% 云侧内部 IPC
    ECP2 -->|"直接调用 / Queue"| EX2
    EX2 -->|"MessageQueue<br/>📬 Shared Memory"| W3
    EX2 -->|"MessageQueue<br/>📬 Shared Memory"| W4
    EX2 -->|"MessageQueue<br/>📬 Shared Memory"| W5
    
    %% Worker 间通信
    W3 -.->|"torch.distributed<br/>🚀 NCCL/HCCL"| W4
    W4 -.->|"torch.distributed<br/>🚀 NCCL/HCCL"| W5
```

---

## 二、总体流程图（含边云通信）

```mermaid
flowchart TD
    U1([🧑‍💻 用户提交普通请求<br/>纯文本 prompt]) --> F1
    
    F1["边侧 API Server 接收请求<br/>Frontend 进程<br/>FastAPI HTTP Handler"] --> F2
    F2["LLMEngine.generate()<br/>创建生成任务<br/>封装 Request 对象"] --> F3
    F3["EngineCoreClient.add_request(request)<br/>📤 准备发送到 EngineCore"] --> NET1
    
    NET1["ZMQ Socket 序列化发送<br/>🌐 input_socket.send_multipart<br/>Msgpack 编码<br/>跨网络传输到云侧"] --> C1
    
    C1["云侧 EngineCoreProc<br/>input_queue.get()<br/>📥 接收请求"] --> C2
    C2["_handle_client_request()<br/>分发请求类型<br/>ADD / ABORT / UTILITY"] --> C3
    C3["EngineCore.add_request(req)<br/>加入 Scheduler.waiting 队列"] --> C4
    
    C4["run_busy_loop()<br/>🔁 EngineCore 主循环"] --> C5
    C5["_process_input_queue()<br/>处理输入队列中的请求"] --> C6
    C6["Scheduler.schedule()<br/>🚀 调度决策<br/>分配 KV Blocks"] --> C7
    C7["构建 SchedulerOutput<br/>封装调度结果"] --> IPC1
    
    IPC1["Executor.execute_model()<br/>📡 通过 MessageQueue 分发<br/>Shared Memory 广播到 Workers"] --> W1
    
    W1["Worker 接收 SchedulerOutput<br/>反序列化输入数据"] --> W2
    W2["execute_model()<br/>⚙️ 模型推理"] --> W3
    W3["InputEmbedding.forward()<br/>token → hidden_states"] --> W4
    W4["Transformer Forward<br/>Attention + FFN"] --> W5
    W5["Sampler.sample()<br/>生成 next_token_id"] --> W6
    W6["组装 ModelRunnerOutput<br/>序列化结果"] --> IPC2
    
    IPC2["Worker Response MQ<br/>📡 MessageQueue.dequeue<br/>Shared Memory 回传结果<br/>到 Executor"] --> C8
    
    C8["EngineCore.step()<br/>获取 Executor 输出"] --> C9
    C9["Scheduler.update_from_output()<br/>更新请求状态<br/>检查是否完成"] --> C10
    C10{"请求是否完成?"} -->|否| C4
    C10 -->|是| C11
    C11["_free_request()<br/>释放 KV Cache"] --> C12
    C12["output_queue.put()<br/>📤 结果放入输出队列"] --> NET2
    
    NET2["ZMQ Socket 序列化发送<br/>🌐 output_socket.send_multipart<br/>Msgpack 编码<br/>跨网络回传边侧"] --> F4
    
    F4["EngineCoreClient 接收输出<br/>📥 output_socket.recv_multipart<br/>反序列化 EngineCoreOutputs"] --> F5
    F5["Frontend 解析输出<br/>token_ids → 文本"] --> F6
    F6{"stream = True?"} -->|是| F7
    F6 -->|否| F8
    F7["SSE 流式推送<br/>逐个 chunk 返回"] --> U2
    F8["组装完整响应<br/>一次性返回"] --> U2
    
    U2([🧑‍💻 用户收到文本响应])
```

---

## 三、Phase 1：边侧接收与请求发送

### 3.1 边侧 Frontend 接收请求

```mermaid
flowchart TD
    A([用户 HTTP POST 请求]) --> B["边侧 API Server<br/>FastAPI / OpenAI-compatible API"]
    B --> C["路由处理函数<br/>/v1/chat/completions"]
    C --> D["解析请求体：<br/>· messages / prompt<br/>· temperature, top_p<br/>· max_tokens<br/>· stream"]
    D --> E["LLMEngine.generate()<br/>或 AsyncLLMEngine.generate()<br/>创建推理任务"]
    E --> F["tokenizer.apply_chat_template()<br/>将对话格式化为 prompt"]
    F --> G["tokenizer.encode(prompt)<br/>生成 prompt_token_ids"]
    G --> H["创建 Request 对象<br/>request_id = uuid<br/>prompt_token_ids<br/>sampling_params"]
    H --> I["EngineCoreClient.add_request(request)<br/>📤 准备跨进程/跨节点发送"]
    I --> J([请求在边侧 Frontend 就绪])
```

**关键函数**：`vllm/v1/engine/llm_engine.py::LLMEngine.generate()`

```python
def generate(self, prompt, sampling_params, request_id):
    # 1. 创建 Request 对象
    request = EngineCoreRequest(
        request_id=request_id,
        prompt_token_ids=prompt_token_ids,
        sampling_params=sampling_params,
        ...
    )
    # 2. 发送到 EngineCore（可能在云侧）
    self.engine_core.add_request(request)
```

---

### 3.2 边侧到云侧的网络发送（ZMQ）

```mermaid
flowchart TD
    A["EngineCoreClient.add_request(request)"] --> B["msgspec.msgpack.encode(request)<br/>📦 Msgpack 序列化 Request 对象"]
    B --> C{"通信模式判断"}
    C -->|InprocClient<br/>同进程| D["直接调用<br/>EngineCore.add_request()"]
    C -->|SyncMPClient /<br/>AsyncMPClient<br/>跨进程/跨节点| E["ZMQ Socket 发送"]
    
    E --> F["input_socket.send_multipart(<br/>[request_type, serialized_data]<br/>)"]
    F --> G{"Socket 类型"}
    G -->|ZMQ IPC<br/>本地| H["Unix Domain Socket<br/>/tmp/vllm-*.sock"]
    G -->|ZMQ TCP<br/>远程| I["TCP Socket<br/>云侧 IP:Port<br/>🌐 跨越网络"]
    
    H --> J([同机进程间传输])
    I --> K([跨节点网络传输<br/>边侧 → 云侧])
```

**关键函数**：`vllm/v1/engine/core_client.py::AsyncMPClient.add_request()`

```python
def add_request(self, request: EngineCoreRequest) -> None:
    # 序列化请求
    serialized = msgspec.msgpack.encode(
        (EngineCoreRequestType.ADD, request)
    )
    # 通过 ZMQ Socket 发送（可能是 TCP 跨网络）
    self.input_socket.send_multipart(
        [EngineCoreRequestType.ADD, serialized],
        copy=False
    )
```

---

## 四、Phase 2：云侧 EngineCore 接收与调度

### 4.1 EngineCore 接收请求

```mermaid
flowchart TD
    A["云侧 EngineCoreProc<br/>run_busy_loop()<br/>🔁 主循环"] --> B["_process_input_queue()<br/>📥 轮询输入队列"]
    B --> C["input_queue.get(block=True)<br/>阻塞等待请求到达"]
    C --> D["反序列化请求<br/>msgspec.msgpack.decode(data)"]
    D --> E["_handle_client_request(<br/>request_type, request<br/>)"]
    E --> F{"request_type?"}
    F -->|ADD| G["EngineCore.add_request(req)<br/>新增请求"]
    F -->|ABORT| H["EngineCore.abort_requests()<br/>取消请求"]
    F -->|UTILITY| I["执行工具函数调用"]
    G --> J["Scheduler.add_request(request)<br/>加入 waiting 队列"]
    H --> K
    I --> K
    J --> K([输入队列处理完毕])
```

**关键函数**：`vllm/v1/engine/core.py::EngineCoreProc._process_input_queue()`

```python
def _process_input_queue(self):
    # 阻塞等待请求
    req = self.input_queue.get(block=True)
    self._handle_client_request(*req)
    
    # 处理队列中剩余请求
    while not self.input_queue.empty():
        req = self.input_queue.get_nowait()
        self._handle_client_request(*req)

def _handle_client_request(self, request_type, request):
    if request_type == EngineCoreRequestType.ADD:
        self.add_request(request)
```

---

### 4.2 Scheduler 调度循环

```mermaid
flowchart TD
    A["EngineCore.step()<br/>执行一个推理 step"] --> B["Scheduler.schedule()<br/>🚀 调度入口"]
    B --> C["self.kv_cache_manager.new_step_starts()<br/>🔄 新 step 开始"]
    C --> D["调度 RUNNING 请求<br/>继续生成未完成的请求"]
    D --> E["调度 WAITING 请求<br/>从 waiting 队列取出新请求"]
    E --> F["KVCacheManager.allocate_slots()<br/>🧱 为新 token 分配物理块"]
    F --> G{"分配成功?"}
    G -->|否| H["_preempt_request()<br/>抢占低优先级请求<br/>释放 KV 空间"]
    G -->|是| I["请求移入 running 队列"]
    H --> D
    I --> J["构建 SchedulerOutput<br/>封装调度结果"]
    J --> K["_update_after_schedule()<br/>推进 num_computed_tokens"]
    K --> L([返回 SchedulerOutput])
```

**关键函数**：`vllm/v1/core/sched/scheduler.py::schedule()`

```python
def schedule(self) -> SchedulerOutput:
    self.kv_cache_manager.new_step_starts()
    token_budget = self.max_num_scheduled_tokens
    
    # 调度 RUNNING
    for req in self.running:
        new_blocks = self.kv_cache_manager.allocate_slots(req, ...)
        if new_blocks is None:
            self._preempt_request(req)  # KV 不足，抢占
    
    # 调度 WAITING
    while self.waiting and token_budget > 0:
        req = self.waiting.pop_request()
        self.running.append(req)
    
    return SchedulerOutput(...)
```

---

## 五、Phase 3：Executor 分发与 Worker 执行

### 5.1 Executor 内部分发（MessageQueue）

```mermaid
flowchart TD
    A["EngineCore 调用<br/>self.model_executor.execute_model(<br/>scheduler_output<br/>)"] --> B["Executor 序列化 SchedulerOutput<br/>提取 input_ids / positions / block_ids"]
    B --> C["Executor._execute_model()<br/>准备广播到 Workers"]
    C --> D{"并行策略"}
    D -->|TP / PP| E["rpc_broadcast_mq.enqueue()<br/>📬 MessageQueue 入队<br/>基于 Shared Memory 广播"]
    D -->|DP| F["按数据并行维度<br/>分发到对应 EngineCore"]
    
    E --> G["各 Worker 的 worker_response_mq<br/>📬 从 MessageQueue 读取数据"]
    F --> G
    G --> H["Worker 反序列化<br/>恢复 Python 对象"]
    H --> I([进入 ModelRunner])
```

**关键函数**：`vllm/v1/executor/multiproc_executor.py`

```python
def _execute_model(self, method, args, kwargs):
    # 通过 MessageQueue 广播到所有 Workers
    self.rpc_broadcast_mq.enqueue((send_method, args, kwargs))
    
    # 从各 Worker 的 Response MQ 收集结果
    for mq in self.response_mqs:
        status, result = mq.dequeue(timeout=...)
    return result
```

---

### 5.2 Worker 模型推理

```mermaid
flowchart TD
    A["Worker.execute_model(<br/>scheduler_output<br/>)"] --> B["解析输入张量：<br/>input_ids [batch, seq_len]<br/>positions [batch, seq_len]<br/>block_ids [batch, num_blocks]"]
    B --> C["self.model.embed_tokens(input_ids)<br/>🔤 Embedding 层查表"]
    C --> D["hidden_states [batch, seq_len, hidden_size]"]
    D --> E["self.model.layers(hidden_states, ...)<br/>🏗️ Transformer 逐层计算"]
    E --> F["Self-Attention：<br/>Q @ K^T / sqrt(d) → softmax → @ V"]
    F --> G["FFN / MLP：<br/>up_proj → activation → down_proj"]
    G --> H["LayerNorm + Residual"]
    H --> I["重复 N 层"]
    I --> J["self.lm_head(hidden_states)<br/>🎯 映射到词表 logits<br/>[batch, seq_len, vocab_size]"]
    J --> K["self.sampler(logits, sampling_params)<br/>🎲 按策略采样"]
    K --> L["sampled_token_ids [batch]<br/>每个请求生成 1 个新 token"]
    L --> M["组装 ModelRunnerOutput<br/>含 sampled_token_ids / logprobs"]
    M --> N([返回 Executor])
```

**关键函数**：`vllm/v1/worker/gpu_model_runner.py::execute_model()`

---

### 5.3 Worker 间通信（TP/PP）

```mermaid
flowchart TD
    A["TP 并行场景<br/>Tensor Parallel Size = 2"]
    
    subgraph W0["Worker 0<br/>GPU 0"]
        A1["Attention 计算<br/>QKV 切分后部分计算"]
    end
    
    subgraph W1["Worker 1<br/>GPU 1"]
        B1["Attention 计算<br/>QKV 切分后部分计算"]
    end
    
    A1 -.->|"torch.distributed.all_reduce<br/>🚀 NCCL/HCCL<br/>汇总 Attention 输出"| B1
    B1 -.->|"all_reduce 结果同步"| A1
    
    A1 --> C["Worker 0 继续 FFN"]
    B1 --> D["Worker 1 继续 FFN"]
    
    C -.->|"all_reduce<br/>汇总 FFN 输出"| D
```

**通信方式**：`torch.distributed.all_reduce` via NCCL (GPU) / HCCL (NPU)

---

## 六、Phase 4：结果回传与边云通信

### 6.1 云侧 EngineCore 更新与输出

```mermaid
flowchart TD
    A["EngineCore.step()<br/>获取 ModelRunnerOutput"] --> B["Scheduler.update_from_output(<br/>scheduler_output,<br/>model_runner_output<br/>)"]
    B --> C["遍历各请求生成的 token"]
    C --> D["_update_request_with_output()<br/>追加 token 到请求输出"]
    D --> E{"_check_stop()<br/>触发停止条件?"}
    E -->|否| F["请求保留在 running<br/>下一 step 继续生成"]
    E -->|是| G["_handle_stopped_request()<br/>处理请求结束"]
    G --> H["_free_request()<br/>释放 KV Cache Blocks"]
    H --> I["finished_req_ids.add(req_id)<br/>标记已完成"]
    I --> J["组装 EngineCoreOutputs<br/>含新生成的 token 和完成状态"]
    J --> K([放入 output_queue])
    F --> L([返回空或部分输出])
```

**关键函数**：`vllm/v1/engine/core.py::EngineCore.step()`

```python
def step(self) -> tuple[dict[int, EngineCoreOutputs], bool]:
    scheduler_output = self.scheduler.schedule()
    model_output = self.model_executor.execute_model(scheduler_output)
    outputs = self.scheduler.update_from_output(scheduler_output, model_output)
    return outputs, True
```

---

### 6.2 云侧到边侧的网络回传（ZMQ）

```mermaid
flowchart TD
    A["EngineCoreOutputs 放入<br/>output_queue"] --> B["EngineCoreProc._process_engine_step()<br/>取出输出"]
    B --> C["msgspec.msgpack.encode(outputs)<br/>📦 序列化输出数据"]
    C --> D["output_socket.send_multipart(<br/>[client_index, serialized_data]<br/>)"]
    D --> E{"Socket 类型"}
    E -->|ZMQ IPC| F["Unix Domain Socket<br/>本地进程间"]
    E -->|ZMQ TCP| G["TCP Socket<br/>🌐 跨越网络<br/>云侧 → 边侧"]
    F --> H([边侧 Frontend 接收])
    G --> H
```

**关键函数**：`vllm/v1/engine/core.py::EngineCoreProc._process_engine_step()`

```python
def _process_engine_step(self) -> bool:
    outputs, model_executed = self.step_fn()
    for output in outputs.items():
        self.output_queue.put_nowait(output)
    return model_executed
```

---

### 6.3 边侧 Frontend 接收与响应

```mermaid
flowchart TD
    A["EngineCoreClient<br/>output_socket.recv_multipart()<br/>📥 接收云侧返回数据"]
    A --> B["msgspec.msgpack.decode(data)<br/>📦 反序列化 EngineCoreOutputs"]
    B --> C["提取 EngineCoreOutput<br/>· request_id<br/>· new_token_ids<br/>· finish_reason"]
    C --> D["tokenizer.decode(token_ids)<br/>将 token 转为文本"]
    D --> E{"stream = True?"}
    E -->|是| F["组装 SSE chunk<br/>data: {...}\\n\\n"]
    E -->|否| G["累加全部文本<br/>组装完整 JSON 响应"]
    F --> H["HTTP Response Body<br/>流式推送"]
    G --> I["HTTP Response Body<br/>一次性返回"]
    H --> U([用户收到文本])
    I --> U
```

**关键函数**：`vllm/v1/engine/core_client.py::AsyncMPClient._output_queue_task()`

```python
async def _output_queue_task(self):
    while True:
        # 从 ZMQ Socket 接收（可能来自网络）
        frames = await self.output_socket.recv_multipart(copy=False)
        client_idx, output_bytes = frames[0], frames[1]
        # 反序列化
        output = msgspec.msgpack.decode(output_bytes, type=EngineCoreOutputs)
        # 分发到对应的请求
        self._handle_output(client_idx, output)
```

---

## 七、完整时序图（含边云通信与进程间数据传输）

```mermaid
sequenceDiagram
    actor User as 用户
    participant EdgeF as 边侧 Frontend<br/>(API Server)
    participant EdgeC as 边侧 EngineCoreClient<br/>(ZMQ Socket)
    participant CloudEC as 云侧 EngineCoreProc
    participant CloudSCH as 云侧 Scheduler
    participant CloudEX as 云侧 Executor
    participant CloudMQ as MessageQueue<br/>(Shared Memory)
    participant Worker as 云侧 Worker
    
    Note over User,Worker: === 边侧 → 云侧：请求发送 ===
    
    User->>EdgeF: POST /v1/chat/completions<br/>prompt: "你好"
    EdgeF->>EdgeF: tokenizer.encode()<br/>创建 Request 对象
    EdgeF->>EdgeC: EngineCoreClient.add_request(req)
    EdgeC->>EdgeC: msgspec.msgpack.encode(req)
    EdgeC->>CloudEC: ZMQ TCP Socket<br/>send_multipart()<br/>🌐 跨越网络
    
    Note over CloudEC,Worker: === 云侧内部处理 ===
    
    CloudEC->>CloudEC: input_queue.get()
    CloudEC->>CloudEC: _handle_client_request(ADD, req)
    CloudEC->>CloudSCH: Scheduler.add_request(req)
    CloudSCH->>CloudSCH: waiting.add_request(req)
    
    loop 调度-执行循环
        CloudEC->>CloudSCH: Scheduler.schedule()
        CloudSCH->>CloudSCH: allocate_slots()<br/>分配 KV Blocks
        CloudSCH-->>CloudEC: SchedulerOutput
        
        CloudEC->>CloudEX: Executor.execute_model()
        CloudEX->>CloudMQ: rpc_broadcast_mq.enqueue()<br/>📬 Shared Memory 广播
        CloudMQ->>Worker: dequeue()<br/>读取调度数据
        
        Worker->>Worker: embed_tokens()
        Worker->>Worker: Transformer Forward
        Worker->>Worker: Sampler.sample()
        Worker-->>CloudMQ: enqueue()<br/>📬 写入结果
        CloudMQ-->>CloudEX: dequeue()<br/>读取 Worker 输出
        CloudEX-->>CloudEC: ModelRunnerOutput
        
        CloudEC->>CloudSCH: update_from_output()
        CloudSCH->>CloudSCH: _check_stop()<br/>检查是否完成
    end
    
    Note over CloudEC,EdgeC: === 云侧 → 边侧：结果回传 ===
    
    CloudEC->>CloudEC: output_queue.put(outputs)
    CloudEC->>EdgeC: ZMQ TCP Socket<br/>send_multipart()<br/>🌐 跨越网络
    EdgeC->>EdgeC: msgspec.msgpack.decode()
    EdgeC-->>EdgeF: EngineCoreOutputs
    
    EdgeF->>EdgeF: tokenizer.decode()<br/>token → 文本
    EdgeF-->>User: "你好！很高兴为你服务..."
```

---

## 八、通信机制汇总表

| 通信链路 | 通信方式 | 数据格式 | 场景 |
|---------|---------|---------|------|
| 用户 ↔ 边侧 Frontend | HTTP / SSE | JSON | 用户请求接入 |
| 边侧 Frontend ↔ EngineCoreClient | Python 函数调用 | Python 对象 | 同进程内 |
| **边侧 EngineCoreClient ↔ 云侧 EngineCoreProc** | **ZMQ TCP Socket** | **Msgpack 二进制** | **🌐 边云跨网络通信** |
| 云侧 EngineCoreProc ↔ Scheduler | 直接调用 | Python 对象 | 同进程内 |
| 云侧 EngineCoreProc ↔ Executor | 直接调用 | Python 对象 | 同进程内 |
| 云侧 Executor ↔ Workers | **MessageQueue** | **Msgpack 二进制** | **📡 进程间共享内存** |
| Worker ↔ Worker (TP) | torch.distributed | CUDA Tensor | GPU 间 NCCL/HCCL |
| 云侧 EngineCoreProc ↔ 边侧 EngineCoreClient | **ZMQ TCP Socket** | **Msgpack 二进制** | **🌐 云侧回传边侧** |
| 边侧 EngineCoreClient ↔ Frontend | Python 函数调用 | Python 对象 | 同进程内 |

---

## 九、数据传输大小估算（普通请求）

| 数据 | 方向 | 大小估算 | 说明 |
|------|------|---------|------|
| Request 对象 | 边侧 → 云侧 | ~1-10 KB | prompt_token_ids + sampling_params |
| SchedulerOutput | Executor → Workers | ~100 KB - 1 MB | input_ids / positions / block_ids |
| ModelRunnerOutput | Workers → Executor | ~10 KB - 100 KB | sampled_token_ids / logprobs |
| EngineCoreOutputs | 云侧 → 边侧 | ~1-10 KB | new_token_ids + finish_reason |
| **KV Cache（P/D 分离时）** | **Prefill → Decode** | **数百 MB - 数 GB** | **每层 K/V 张量 × seq_len × hidden_size** |

> 注：普通纯文本请求的 prompt 和输出 token 数据量较小，跨网络传输开销低。若系统启用 P/D 分离，KV Cache 的传输是主要带宽消耗来源。

---

## 十、vllm-ascend 在边云场景中的扩展

| 扩展 | 通信影响 | 说明 |
|------|---------|------|
| **动态批处理** | 云侧 Scheduler 内部 | `BudgetRefiner` 动态调整 batch size，不影响边云通信 |
| **负载均衡 (DP)** | 云侧 ↔ 云侧 / 边侧 ↔ 云侧 | `BalanceScheduler.balance_gather()` 通过 `torch.distributed.all_gather` 同步各 rank 负载 |
| **重计算调度** | 云侧内部 + 边云 | KV Consumer 内存不足时，`RecomputeScheduler` 将请求回退为 `EngineCoreOutput(finish_reason=STOP)`，经 ZMQ 回传边侧后重新路由 |
| **NPU Worker** | Worker 内部 | `NPUModelRunner` 将 CUDA 算子替换为 CANN 算子，边云通信链路不变 |
